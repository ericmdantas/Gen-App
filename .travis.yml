language: node_js

node_js:
  - "stable"

before_script:
  - npm install -g gulp-cli mocha
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"

script:
  - npm run test-client
  - npm run test-server

services: mongodb
env:
 - CXX=g++-4.8
addons:
 apt:
  sources:
   - ubuntu-toolchain-r-test
  packages:
   - g++-4.8enter code here